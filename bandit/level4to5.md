# 파일 형식

## 파일 형식

(1) ASCII text 파일
- ascii 코드로 되어 사람이 직접 읽을 수 있는 파일이다.

(2) data 파일
- 일반적으로 응용 프로그램이 사용하는 바이너리 또는 구조화된 파일이다.

(3) 파일 형식 확인하기
```bash
file [파일명]
```
- data 파일 결과
  ```bash
  file [파일명1]
  [파일명1]: data
  ```

- ASCII text 파일
  ```bash
  file [파일명2]
  [파일명2]: ASCII text
  ```
