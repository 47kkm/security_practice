# Level 16 --> 17

## 문제 개요
현재 비밀번호를 이용하여 localhost 31000-32000 포트 범위 중 다음의 조건을 만족하는 포트에 접근하여 다음 레벨 인증정보를 획득할 수 있다.
- 여러 포트가 열려 있다.
- 열려 있는 포트 중 일부는 SSL/TLS 를 사용한다.
- SSL/TLS 사용 포트 중 인증정보를 응답하는 포트는 하나이며, 나머지는 전송 메시지를 그대로 돌려준다.

## 풀이 과정
1. ssh 서버 로그인
2. `nmap` 을 이용해 31000-32000 포트 스캔하여 열려 있는 포트 확인
3. `-sV` 옵션으로 서비스 유형 분석 및 SSL/TLS 사용하는 포트 식별
4. `ncat` 을 이용하여 현재 비밀번호를 전송하고, 인증정보를 응답하는 포트 식별
5. 응답받은 인증정보를 `/tmp/***`로 복제
6. `scp`를 이용해 복제한 인증정보를 내려 받음
7. 내려 받은 인증정보(`rsa`)를 이용해 다음 레벨에 접근 가능

## 핵심 개념
1. nmap
```bash
nmap [호스트]
```
- 포트 오픈 여부, 구동중인 서비스 등을 알 수 있는 도구
- `nmap [호스트]` 결과
  - 어떤 포트가 열려 있는지 (`open`) --> 연결 가능 (서비스 실행 중)
  - 닫혀 있는지 (`closed`) --> 포트는 있지만 닫힘
  - 막혀 있는지 (`filtered`) --> 방화벽 때문에 확인 불가

2. nmap 주요 옵션
```bash
nmap -p [포트번호] [호스트]
nmap -sV [호스트]
nmap -O [호스트]
nmap -sS [호스트]
nmap -sU [호스트]
nmap --script [스크립트(NSE)] [호스트]
```
- `-p` - 포트 지정하여 스캔
  - `[포트번호]` - 단일 포트 스캔
  - `[포트번호, 포트번호, ...]` - 다중 포트 스캔
  - `[포트번호-포트번호]` - 범위 지정
- `-sV` - 포트 열림 + 어떤 서비스인지 확인 (HTTP, SSH, MySQL 등)
- `-O` - OS 스캔
- `-sS` - 스텔스 스캔 (root 권한 필요)
- `-sU` - UDP 스캔 (DNS, SNMP 같은 UDP 확인)
- `--script` - nmap 의 자체 스크립트인 NSE 실행. 카테고리(tag)가 붙어있다.
  - `default` - 기본적으로 안전하고 유용한 스크립트
  - `safe` - 서비스에 영향이 거의 없음
  - `auth` - 인증 관련
  - `vuln` - 취약점 검사
  - `discovery` - 정보 수집
  - `ssl` - SSL/TLS 관련
  - `brute` - 무차별 대입

cf) 어떤 스크립트가 있는지 찾기
- 리스트 보기
  ```bash
  ls /usr/share/nmap/scripts/
  ```
- 특정 키워드(태그) 검색
  ```bash
  ls /usr/share/nmap/scripts/ | grep ssl
  ls /usr/share/namp/scripts/ssl-*.nse
  ```

3. 사용 예시
```bash
# 192.168.0.10 호스트의 5000-6000 범위 포트 중에서 SSL/TLS 통신을 하는 포트 검사
$ nmap -p 5000-6000 --script ssl-cert 192.168.0.10
```

## 보안 관점 분석
- `nmap`을 이용한 서비스 탐지 기반 공격

## 위험 요소
- 열린 포트 + 서비스 노출로 공격 표면이 확대 된다.
- 포트가 많을수록 공격 기회가 증가한다.

## 대응 방안
- 불필요 포트 차단
- 포트 스캐닝 탐지