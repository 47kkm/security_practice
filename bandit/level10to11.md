# base64

## base64 명령어 사용하기

(1) base64
```bash
base64 [옵션] [파일명]
```
- 문자열을 base64로 인코딩 하거나 base64를 문자열로 디코딩 해주는 명령어

(2) base64 주요 옵션
```bash
base64 -d data.txt
```
- `-d`- base64 데이터를 문자열로 디코딩
- 아무 옵션이 없으면 문자열을 base64 로 인코딩한다.